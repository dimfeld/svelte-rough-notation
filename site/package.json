{
  "name": "svelte-rough-notation-site",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf public/build",
    "build": "yarn run clean && rollup -c",
    "dev": "yarn run clean && rollup -c -w",
    "start": "sirv public -H 0.0.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^12.0.0",
    "@rollup/plugin-node-resolve": "^8.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "rollup": "^2.16.1",
    "rollup-plugin-livereload": "^1.3.0",
    "rollup-plugin-svelte": "^5.0.3",
    "rollup-plugin-terser": "^5.1.2",
    "svelte": "^3.49.0"
  },
  "dependencies": {
    "rough-notation": "^0.4.0",
    "sirv-cli": "^0.4.4",
    "svelte-rough-notation": "../"
  }
}
